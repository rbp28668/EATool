/**
 * This package provides an in memory persistence layer.  It's close to the original in memory graph in concept except that
 * in a lot of cases, references to objects are contained in proxies that allow lazy loading from the model or meta-model.
 */
package alvahouse.eatool.repository.persist.memory;

