/**
 * Factory classes to deserialise elements of the meta model.
 * Note that these no longer try to build a complete connected model but instead set keys
 * to support lazy loading from the persistence layer.
 */
package alvahouse.eatool.repository.metamodel.factory;

